# Raw env file: paste the bcrypt hash literally, including single $ signs.
# Generate interactively: docker run --rm -it caddy:2-alpine caddy hash-password
DASHBOARD_USER=dashboard
DASHBOARD_PASSWORD_HASH=REPLACE_WITH_BCRYPT_HASH
