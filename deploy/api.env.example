# Raw env file: no quotes, no $$ escaping. Keep chmod 600 on the VPS.
DB_HOST=postgres
DB_PORT=5432
DB_NAME=enterprise_dashboard
DB_USER=enterprise_dashboard
DB_PASSWORD=REPLACE_WITH_DATABASE_PASSWORD
TALLY_INGEST_TOKEN=REPLACE_WITH_AT_LEAST_32_RANDOM_CHARACTERS
