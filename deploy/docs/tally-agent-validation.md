# Tally sender validation

Confirmed Tally source controls on 2026-09-12 (`tally-008`): new server logs identified 24 failures at `&#4;` references and one literal-control failure; the user's saved response scan found U+0005 in voucher class names, and saved GROUP XML confirmed `<ADDLALLOCTYPE>&#4; Not Applicable</ADDLALLOCTYPE>`. A narrow source-parser adapter accepts only these two confirmed controls as literals/references, preserving U+0004/U+0005 through JSON. No source values are stripped, replaced with labels, or interpreted as accounting values. Strict syntax checks remain for invalid tag/attribute names, other unsupported controls, NUL, entities, DTDs and truncated XML.

All 30 sender tests passed, including single-character stream splits, decimal/hex/padded references, literal controls in attributes/text/CDATA, preservation of escaped reference-looking text, and XML 1.0 Unicode text behavior. All three focused PostgreSQL source API tests passed, including source XML -> agent -> authenticated HTTP -> JSONB -> exact U+0004/U+0005 comparisons. The disposable local PostgreSQL container was stopped afterward. The checksum-verified extracted tally-008 ZIP passed the full 13-collection fixture with both control forms and a second fixture confirming NUL still fails with detailed diagnostics. No production data was uploaded. A full server dry run is still required to identify any further compatibility or capacity limits beyond the confirmed cases. No new API/schema version or migration is required for this correction.

Export diagnostics on 2026-09-12 (`tally-007`): the supplied real-server dry-run log discovered five companies, recorded 25 collection XML failures and retained 39 source records in partial previews. The old generic message does not establish the exact XML defect; no raw customer response was available. Added shared diagnostics for discovery/source exports, response metadata, 10-second progress, nested network codes, parse/decode/record-handler stages, XML category/path/line/column and bounded numeric-character-reference details. Source collection failures include discarded-prefix counts; final run logs summarize failed collections. No source data is stripped or automatically repaired.

All 27 sender tests passed; the four focused diagnostic tests passed again after numeric-reference cursor handling was added. Both extracted `tally-007` ZIP tests passed: normal source capture and an intentional `&#4;` XML failure. The actual CMD launcher reported the failure code, offending reference and field path, preserved partial coverage, and exited 1. Tests used local fixtures, paths containing spaces and bundled Node with no system Node on PATH. Tokens, query parameters, raw parser/network messages and accounting values are excluded from the new transport logs. The server must run this bundle to determine the specific cause of its failed collections; the production XML failures are not claimed resolved.

Source archive implementation on 2026-09-12 (`tally-006`): source mode is now the default and bypasses all legacy amount/date/voucher transformations. The 22-test sender suite passed, followed by six focused source tests including a newly added durable-outbox resume test (23 distinct sender tests covered). Ten API tests passed against a newly created isolated PostgreSQL 16 database on localhost:55436. After adding company-routing validation and an end-to-end test, all three focused source API tests passed. Coverage includes idempotent migration 004, JSONB preservation of empty/nonstandard amounts and dates, repeated nested entries, atomic rollback, immutable retries before/after completion, exact counts/ordinals, company isolation, wrong company identity rejection, partial captures and latest-complete ordering. The sender-to-authenticated-HTTP-to-PostgreSQL test stored original source values without updating dashboard tables. No business-field normalization is performed in source mode.

The checksum-verified `tally-006` ZIP passed an extracted `Run-Sync.cmd --dry-run` test in a path with spaces and no Node on PATH. The simulator served all 13 catalog collections (8 source records, including empty/nonstandard amounts); the output JSON preserved those values. Packaged core scripts match the checkout. Caddy runtime validation could not execute: both the existing finance UI image and refreshed official caddy:2-alpine report `exec /usr/bin/caddy: exec format error` despite amd64 metadata matching the local engine. HTTP authentication tests passed; live Caddy acceptance remains a deployment gate. New deployment checks assert migration 004 and source route availability before promotion. No production deployment, real Tally full-profile acceptance, production source upload or report reconciliation was performed. The temporary local PostgreSQL test container is stopped after verification.

Explicit empty ledger balance correction on 2026-09-12 (`tally-005`): the server diagnostic confirmed the fifth ledger's `CLOSINGBALANCE` was a present empty string. Ledger mapping now converts only that representation to `0.00`, following Tally's documented empty Amount convention; absent/null/object/repeated values and malformed amounts still fail, and generic amount/voucher behavior remains strict. All 17 agent tests passed. Built and checksum-verified the new ZIP; its extracted CMD launcher passed the local simulator test with an empty typed closing-balance element, 5 ledgers and 2 vouchers, and an assertion that the preview contains `0.00`. No real server upload or complete real-company reconciliation was performed. Re-run the server dry run before enabling uploads.

XML discovery correction on 2026-09-12 (`tally-004`): all 16 agent tests passed. New synthetic regressions cover header counters and description collections excluded from discovery, five companies with typed NAME/GUID fields, typed ledger/voucher fields, preservation of repeated accounting entries, conflicting names and missing identity rejection, and a two-company dry run verifying `SVCURRENTCOMPANY` on every ledger/voucher export and subsequent identity checks. These shapes reproduce the reported empty record and NAME arrays; no raw customer XML was available to confirm the precise server response. The parser now reads only direct records in `ENVELOPE/BODY/DATA/COLLECTION` and reads typed leaf text independently of attributes.

Built `deploy/bundles/tally-004/FinanceTallyAgent-tally-004-win-x64.zip` using the existing bundler and its checksum-verified Node.js 22.23.2 runtime. Verified the ZIP checksum and ran the extracted `Run-Sync.cmd --dry-run` in a path containing spaces with Node removed from PATH. A local simulator supplied header counters, repeated NAME metadata and typed scalar fields: extraction succeeded with 5 ledgers and 2 vouchers. Packaged token was empty and fixture tools absent. Test worker/launcher process creation required execution outside the sandbox after `spawn EPERM`. No production API upload, server installation, scheduled task or live Tally acceptance was performed. Preserve server `config.json`, `token.txt` and the complete `state` directory on upgrade, then repeat the server dry run.

Built-in config credential entry on 2026-09-12: twelve tests passed, adding paired-credential validation, exclusive login-mode validation, built-in helper invocation, suppression of credentials in events, and post-login XML readiness gating. The Windows UI Automation script parsed successfully and the required Automation.Compare API is present locally. The `tally-003` bundle includes this script and blank username/password fields. No actual Tally GUI login or credentials were exercised: control discovery, writable Value-pattern support, submission and interactive-session behavior still need verification on the Tally server. Earlier external-helper-only limitations below describe the previous bundle.

Configurable startup validation on 2026-09-12: ten sender/fixture/launcher/startup tests passed, covering opt-in behavior, readiness reuse, required-company polling, timeout, remote-host refusal, configured login-helper invocation, and a real Windows PowerShell process check against the already-running test Node executable. No Tally process was launched and no live login or VPS upload was attempted. The actual Tally executable path and optional credential-entry helper remain installation-specific. The `tally-002` bundle adds these settings, disabled by default.

Portable package validation on 2026-09-11: built `deploy/bundles/tally-001/FinanceTallyAgent-tally-001-win-x64.zip` with the pinned Node.js 22.23.2 Windows x64 runtime, verified against the official SHA-256 manifest. All six sender/fixture/launcher tests passed. An extracted ZIP in a path containing spaces ran `Run-Sync.cmd --dry-run` with Node removed from PATH, extracted 5 ledgers and 2 vouchers from a local XML simulator, and wrote a successful run log. The packaged token file was verified empty and fixture tools were absent. No production uploads were made during this packaging test. Actual server execution and Task Scheduler policy remain environment-specific.

Validated locally on 2026-09-11:

- Eight API tests passed against an isolated PostgreSQL 16 container with database `enterprise_dashboard_test`. Migration 003 applied alongside existing migrations; existing ingestion tests still pass.
- A 50,100-voucher snapshot exceeding 20 MiB was uploaded in 51 chunks and finalized with the exact row count. This is a functional scale test, not a VPS throughput benchmark.
- Missing chunks, changed retry payloads, mismatched manifest totals and cross-chunk duplicate ledgers were rejected. Failed finalization preserved existing live data. Completed batches returned repeatable acknowledgements.
- HTTP tests checked authentication before body parsing for each new machine endpoint.
- Four sender tests passed: exact decimal handling and invalid dates; streamed XML/truncation/error detection; bounded chunks and identical retries after a lost acknowledgement; full-run persistence and resume after failure, without logging the token.
- PowerShell runner parsed successfully. No Task Scheduler job was created.

Not validated: actual TallyPrime exports, company availability/session behavior, custom voucher/accounting conventions, server-side Windows execution and target-volume runtime. Perform a dry run and reconcile against Tally before live uploads.

The local `caddy:2-alpine` image could not execute (`exec /usr/bin/caddy: exec format error`), so the new UI Caddy route configuration was not runtime-validated locally. Deployment validates the Caddy configuration before starting the release. No VPS changes or production imports were executed during implementation.
