PORT=3000
DB_USER=sa
DB_PASSWORD=
DB_SERVER=localhost
DB_NAME=EnterpriseDashboard

# Tally HTTP/XML (enable in Tally: F12 > Advanced Configuration)
# Use the Tally Server machine IP when data lives on the server.
# Use localhost or the accountant PC IP when Tally runs only on a local machine.
TALLY_HOST=localhost
TALLY_PORT=9000
TALLY_TIMEOUT_MS=8000
